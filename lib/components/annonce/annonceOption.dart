import 'package:flutter/cupertino.dart';

class AnnonceOption extends StatelessWidget {
  const AnnonceOption({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("HELLO"),
    );
  }

}